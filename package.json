{
  "name": "calendar-app",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "npx concurrently -n frontend,backend -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "db:check": "npm run db:check --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
